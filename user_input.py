# user_input.py

# Ask for the user's name
# Use the input() function to prompt the user for their name and store the response in a variable called name.
name = input("What's your name? ")

# Ask for the user's age
# Use the input() function to prompt the user for their age and store the response in a variable called age.
age = input("How old are you? ")

# Ask for the user's location
# Use the input() function to prompt the user for their location and store the response in a variable called location.
location = input("Where do you live? ")

# Print a personalized message
# Use the print() function to display a message that includes the user’s name, age, and location.
print(f"Hello {name}, you are {age} years old and live in {location}.")

# Save and Run the Program:
# Run the program to ensure it works correctly. You can do this by opening a terminal or command prompt; 
# Navigate to the directory where your file is saved, and run:
# python user_input.py

